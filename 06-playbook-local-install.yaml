- name: local nginx install
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: nginx install
    ansible.builtin.package:
     name: nginx
     state: present

  - name: nginx start
    dnf:
     name: nginx
     state: started
     enabled: yes

