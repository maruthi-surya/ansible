- name: install and start nginx
  hosts: frontend
  become: yes
  tasks:
  - name: nginx install
    ansible.builtin.package:
     name: nginx
     state: present

  - name: start nginx
    ansible.builtin.service:
     name: httpd
     state: started
     enabled: yes