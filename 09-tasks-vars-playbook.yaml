- name: task level vars calling
  hosts: local
  connection: local
  vars:
   name: john
  tasks:
  - name: task level vars calling
    vars:
     name: joe
    ansible.builtin.debug:
     msg: "{{ name }}" 
